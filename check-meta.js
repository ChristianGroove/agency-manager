const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('integration_connections').select('credentials, metadata').eq('id', '14503897-ffc0-4488-b3fe-0bdbb206322a').single().then(async (res) => { const token = res.data.credentials.access_token; const waba = res.data.metadata.waba_id; console.log('WABA:', waba); const r = await fetch('https://graph.facebook.com/v24.0/'+waba+'/subscribed_apps', { headers: { Authorization: 'Bearer '+token } }); console.log('STATUS:', r.status); console.log(await r.text()); });
