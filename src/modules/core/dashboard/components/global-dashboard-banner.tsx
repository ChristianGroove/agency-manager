"use client"

import React from "react"

/**
 * Global Dashboard Banner
 * Este componente es un slot reservado para futuros avisos globales o publicidad (Sponsorships / Avisos del Sistema).
 * Vivirá en la parte superior de todos los dashboards sin importar el "Space".
 */
export function GlobalDashboardBanner() {
    return (
        null // <div className="w-full bg-primary/10 text-primary py-2 text-center text-sm font-bold">Banner Global Slot</div>
    )
}
